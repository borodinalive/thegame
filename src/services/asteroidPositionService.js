'use strict';

export default AsteroidPositionService;

class AsteroidPositionService {
  constructor(divider) {
    this.divider = divider;
  }

  setAsteroidPosition() {
  }

  getAsteroidPosition() {
  }
}
